# ═══════════════════════════════════════════════════════════════════
# QgisRemoteMCP — Python dependencies
# ═══════════════════════════════════════════════════════════════════

# MCP Protocol
mcp>=1.9.0

# HTTP server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Data models
pydantic>=2.10.0

# HTTP client (for vision service proxying)
httpx>=0.28.0
aiofiles>=24.1.0

# Auth
PyJWT>=2.9.0

# Multi-user: Docker SDK (only needed when MULTI_USER_MODE=true)
docker>=7.0.0

# Utilities
python-dotenv>=1.0.0
orjson>=3.10.0

# Geo (lightweight, for GeoJSON handling in MCP server)
geojson>=3.1.0
